
import { toLink } from 'pilas-engine-bloques/helpers/to-link';
import { module, test } from 'qunit';

module('Unit | Helper | to link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toLink([42]);
  assert.ok(result);
});

