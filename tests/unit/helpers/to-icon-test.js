
import { toIcon } from 'pilas-engine-bloques/helpers/to-icon';
import { module, test } from 'qunit';

module('Unit | Helper | to icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toIcon([42]);
  assert.ok(result);
});

