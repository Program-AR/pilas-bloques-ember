{{outlet}}
<div class="contenido-principal">
  <div class="contenedor-acercade">
    <PilasAcercaDe />
    <p>
      Actualmente estás usando la versión
      <code>
        {{app-version}}
      </code>
      {{#if hayActualizacion}}
        (y se puede actualizar a la versión{{versionMasReciente}})
      {{/if}}
    </p>
    <p>
      <button class="btn btn-warning" {{action "visitarWeb"}}>
        {{fa-icon "globe"}}
        Visitar la web del proyecto
      </button>
      <PilasUpdate />
    </p>
  </div>
</div>