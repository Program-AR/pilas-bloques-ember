<Breadcrumb>
    <BreadcrumbItem class="challenge-title" @selected={{true}} @href={{href-to "desafio" desafio.id}}>
        {{desafio.titulo}}
    </BreadcrumbItem>
    {{#if desafio.grupo.titulo}}
        <BreadcrumbItem @href={{href-to "libros.verLibro" desafio.grupo.capitulo.libro.id}}>
            {{desafio.grupo.titulo}}
        </BreadcrumbItem>
    {{/if}}
    <BreadcrumbItem @href={{href-to "libros.verLibro" desafio.grupo.capitulo.libro.id}}>
        {{desafio.grupo.capitulo.titulo}}
    </BreadcrumbItem>
    <BreadcrumbItem @href={{href-to "libros.verLibro" desafio.grupo.capitulo.libro.id}}>
        {{desafio.grupo.capitulo.libro.titulo}}
    </BreadcrumbItem>
    <BreadcrumbItem @initial={{true}} @href={{href-to "index"}}>
        {{paper-icon "home"}}
    </BreadcrumbItem>
</Breadcrumb>