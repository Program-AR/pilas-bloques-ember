{{#unless cargando}}
    <PaperCard class={{if shouldUseFloatingMode "exercise-buttons-collapsed-card exercise-actions flex layout-column" "exercise-buttons-card exercise-actions flex layout-row"}}>
        
        <SceneButton 
            @class="step-by-step-button-container"
            @element-id="step-by-step-run-button"
            @label="Ejecutar paso a paso"
            @icon="play_arrow"
            @isHidden={{or (not needShowStepByStepButtons) running}}
            @isDisabled={{running}} 
            @isMini={{true}} 
            @onClick={{step-by-step-execution}}
            @tooltip="Ejecutar paso a paso la solución propuesta"
        />

        <SceneButton 
            @class="step-by-step-button-container"
            @element-id="run-step-button"
            @label="Avanzar un paso"
            @icon="skip_next"
            @isHidden={{or (not needShowStepByStepButtons) (not running)}}
            @isDisabled={{not isPausedOnBreackpoint}} 
            @isMini={{true}} 
            @onClick={{step}}
            @tooltip="Avanzar un paso" 
        />

        <SceneButton 
            @class="run-button-container"
            @element-id="run-button"
            @label="Ejecutar"
            @icon="play_arrow"
            @isHidden={{running}}
            @isDisabled={{running}} 
            @isMini={{true}} 
            @onClick={{run}}
            @tooltip="Ejecutar la solución propuesta"
        />

        <SceneButton 
            @class="restart-button-container"
            @element-id="restart-button"
            @label="Reiniciar"
            @icon="refresh"
            @isHidden={{not running}}
            @isDisabled={{not running}} 
            @isMini={{true}} 
            @onClick={{reset}}
            @tooltip="Reiniciar la ejecución" 
        />

        {{#unless shouldUseFloatingMode}}
            <span class="flex"></span>
        {{/unless}}

        <SceneButton 
            @class="toggle-container-collapsed"
            @element-id="turbo-button" 
            @faIcon="fa fa-bolt"
            @isHidden={{false}}
            @isDisabled={{running}} 
            @isMini={{true}} 
            @onClick={{updateTurboMode}}
            @tooltip="Modo Turbo" 
            />

    </PaperCard>
{{/unless}}