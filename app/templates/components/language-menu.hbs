<PaperMenu @offset="5 43" as |menu|>
    <PaperTooltip>
        {{t "changeLanguage"}}
    </PaperTooltip>

    <menu.trigger>
        <RPaperButton id="language-menu" class="md-menu-origin" @iconButton={{true}}>
            <RPaperIcon @iconName="language"/>
        </RPaperButton>
    </menu.trigger>

    <menu.content as |content|>
        {{#each localeCodes as |localeCode|}}
        <content.menu-item>
            <RPaperButton @onClick={{action "setLocale" localeCode }}>
                <span class="menu-item-typography">{{compute (action languageName) localeCode}}</span>
            </RPaperButton>
        </content.menu-item>
        {{/each}}


    </menu.content>
</PaperMenu>