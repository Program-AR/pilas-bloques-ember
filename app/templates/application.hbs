{{#if layout}}
  {{partial "navbar"}}
  {{partial "modal"}}
{{else}}
  <style>
    .absolute {
      top: 10px;
    }
  </style>
{{/if}}

<div class="page-container">
  {{outlet class='absolute'}}
</div>
