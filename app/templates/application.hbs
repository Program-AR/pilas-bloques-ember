<script>

window.anterior_altura = 0;


function redimensionar() {
  var panel = document.getElementById('panel-derecho');

  if (!panel)
    return;

  var altura = panel.getClientRects()[0].height;

  if (window.anterior_altura != altura) {
    var e = document.getElementById('contenedor-blockly');
    e.style.height = (altura - 50) + 'px';
    window.anterior_altura = altura;
    Blockly.fireUiEvent(window, 'resize');
  }
}

function forzar_redimensionado() {
  window.anterior_altura += 1;
  redimensionar();
}

window.onresize = redimensionar;

</script>


<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">pilas-engine-bloques</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#link-to "index" tagName='li'}}<a href=#>index</a>{{/link-to}}
        {{#link-to "editor" tagName='li'}}<a href=#>editor</a>{{/link-to}}
        {{#link-to "preferencias" tagName='li'}}<a href=#>preferencias</a>{{/link-to}}
      </ul>

      <div class='header-right'>
        <button class='btn btn-default btn-xs btn-flat' {{action "actualizar"}}><i class='glyphicon glyphicon glyphicon-refresh'></i></button>
        <button class='btn btn-default btn-xs btn-flat' {{action "mostrar_devtools"}}><i class='glyphicon glyphicon glyphicon-cog'></i></button>
        <button class='btn btn-default btn-xs btn-flat' {{action "show"}}><i class='glyphicon glyphicon-star-empty'></i></button>
      </div>
    </div>
  </div>
</nav>


{{outlet}}



{{#bs-modal name="myModal" fade=true footerButtonsBinding="myModalButtons" title="Acerca de..."}}
    <p><strong>pilas-engine-bloques</strong> es un prototipo
    que integra <a href='https://developers.google.com/blockly/' target='_blank'>blockly</a> junto a <a href='http://www.pilas-engine.com.ar/' target='_blank'>pilas-engine</a>.

    El objetivo del prototipo es realizar una aplicación
    que permita a los jóvenes realizar sus primeros pasos
    en programación de manera sencilla.</p>

    <p>Estás utilizando la versión <code>0.1.4</code>.
{{/bs-modal}}
