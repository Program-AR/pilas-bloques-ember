/*test('Movimientos en cuadricula', function(assert) {
  var done = assert.async();
  expect(8);

  pilas = new Pilas();
  pilas.iniciar({ancho: 320, alto: 240, data_path: '../src/data'});

  pilas.onready = function() {
    var cuadricula = new Cuadricula(0, 0, 3, 5,
         { alto: 300, ancho: 200 },
         //implica casilla.ancho==40 && casilla.alto==100

    pilas.ejecutar();
  }
 test('Reubicar casilla', function(assert) {
       assert.equal(casilla.y,0);
       assert.equal(casilla2.x,-40);
       assert.equal(casilla2.y,50);
      done();
  };

});*/
